<form method="post" action="{{ path('app_bon_livraison_delete', {'id': bon_livraison.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bon_livraison.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
